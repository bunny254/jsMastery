const fruits = ['Apple', 'Orange', 'Strawberry'];
//Completely empty an array
fruits.length = 0;

//Remove the 3rd indexed array element
fruits.length = 2;


//Sparse an array --3 fruits and 2 other empty items
fruits.length = 5;

console.log(fruits); // []