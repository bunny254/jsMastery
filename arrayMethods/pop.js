//What pop does is to remove the last element from an array

const numbers = [10, 20, 30, 40];
const last = numbers.pop();


console.log(`numbers array:[${numbers}]  with length of ${numbers.length}`);
